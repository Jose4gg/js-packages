import { Buttons } from '../../types';

import adp from './adp';
import nayya from './nayya';
import upwise from './upwise';

const buttons: { adp: Buttons; nayya: Buttons; upwise: Buttons } = {
  nayya,
  adp,
  upwise,
};

export { buttons };
