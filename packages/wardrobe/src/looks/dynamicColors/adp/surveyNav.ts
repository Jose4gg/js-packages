import { SurveyNavColors } from '../../../types';
import { palette } from '../../palette';

const adpPalette = palette.adp;

export const surveyNav: SurveyNavColors = {
  link: adpPalette.isabelline[500],
  activeLink: adpPalette.primary[500],
  visitedLink: adpPalette.isabelline[500],
};
