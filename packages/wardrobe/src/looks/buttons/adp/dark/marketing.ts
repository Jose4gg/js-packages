import { ButtonStyles } from '../../../../types';

const buttonDarkMarketingStyles: ButtonStyles = {
  borderRadius: '1.6875rem',
};

const buttonDarkMarketing: { styles: ButtonStyles } = {
  styles: buttonDarkMarketingStyles,
};

export default buttonDarkMarketing;
