import { GenericColors } from '../../../types';
import { palette } from '../../palette';

const nayyaPalette = palette.nayya;

export const generics: GenericColors = {
  supportive: nayyaPalette.isabelline[600],
};
