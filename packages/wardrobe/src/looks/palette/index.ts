import { Palette } from '../../types';

import adp from './adpPalette';
import nayya from './nayyaPalette';
import upwise from './upwisePalette';

const palette: {
  upwise: Palette;
  adp: Palette;
  nayya: Palette;
} = {
  upwise,
  adp,
  nayya,
};

export { palette };
