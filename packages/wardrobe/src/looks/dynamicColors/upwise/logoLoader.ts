import { LogoLoaderColors } from '../../../types';
import { palette } from '../../palette';

const nayyaPalette = palette.nayya;

export const logoLoader: LogoLoaderColors = {
  borderColor: nayyaPalette.isabelline[100],
  spinnerColor: nayyaPalette.sinbad[500],
};
