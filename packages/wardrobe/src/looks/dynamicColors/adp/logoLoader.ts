import { LogoLoaderColors } from '../../../types';
import { palette } from '../../palette';

const adpPalette = palette.adp;

export const logoLoader: LogoLoaderColors = {
  borderColor: adpPalette.isabelline[100],
  spinnerColor: adpPalette.sinbad[500],
};
